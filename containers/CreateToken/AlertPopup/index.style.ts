import { Box } from '@chakra-ui/react';
import styled from '@emotion/styled';

const StyledAlertPopup = styled(Box)`
  display: flex;

  .view-btn {
    background: transparent;
    border: 1.2px solid #8a8986;
    width: 55px;
    height: 24px;
    margin-top: 10px;
  }
`;

export { StyledAlertPopup };
